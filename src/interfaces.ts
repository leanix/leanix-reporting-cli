export interface LxrConfig {
  host: string;
  workspace: string;
  apitoken: string;
}

export interface UserInitInput {
  name: string;
  id: string;
  author: string;
  title: string;
  description: string;
  host: string;
  workspace: string;
  apitoken: string;
}